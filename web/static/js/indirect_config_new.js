// 加载form 和 element模块
layui.use(['form', 'element'], function(){
    var form = layui.form
    ,element = layui.element

    // 点击新增配置时
    form.on('submit(indirect_cfg_new)', function(data) {

    });
});